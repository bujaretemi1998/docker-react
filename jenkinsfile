pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'docker run node:16-alpine npm install'
            }
        }
        stage('Test') {
            steps {
                sh 'docker run node:16-alpine npm test'
            }
        }
    }
}
